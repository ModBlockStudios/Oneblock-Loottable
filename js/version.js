/* ------------------------------------------------------------------ *
 * Source unique de vérité pour la version de l'interface.
 *
 * À METTRE À JOUR à chaque modification notable :
 *   - `version`   : numéro SemVer de l'interface (MAJEUR.MINEUR.CORRECTIF)
 *   - `buildDate` : date de build (AAAA-MM-JJ)
 *
 * Affiché dans l'en-tête du site pour vérifier en un coup d'œil
 * si la version en ligne est bien à jour.
 * ------------------------------------------------------------------ */
window.APP_INFO = {
  version: '1.1.0',
  buildDate: '2026-06-30',
};
